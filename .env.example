# Required for AI-powered ebook generation
ANTHROPIC_API_KEY=sk-ant-...

# Required for cover image generation
REPLICATE_API_TOKEN=r8_...

# Data directory for generated ebooks (default: /tmp/ebook-gen-data)
DATA_DIR=/data

# Server config
PORT=3000
NODE_ENV=production
